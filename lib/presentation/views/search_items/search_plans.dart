import 'package:flutter/material.dart';

class GetPlans extends StatelessWidget {
  const GetPlans({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
